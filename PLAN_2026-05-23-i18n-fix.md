# PLAN — Fix i18n bug + thêm tiếng Trung

**Ngày:** 2026-05-23 16:40
**Đại ca báo bug**: Chọn ngôn ngữ KHÔNG đổi liền — phải thoát app mới đổi, mà đổi được có dashboard, còn lại vẫn tiếng Việt. Muốn:
1. Có nút "Chọn rồi đổi luôn" — đổi xong reload toàn app
2. Thêm tiếng Trung 🇨🇳
3. App phải đổi NGAY không cần thoát

---

## 1. Root cause bug

### Bug A — Một số chỗ KHÔNG đổi
**Nguyên nhân**: Em chỉ áp `t()` + `useLocale()` vào 4 file (tab _layout, tab Nhập, Settings hub, SafeToSpendCard). Còn ~30 file khác vẫn dùng string tiếng Việt cứng → không đổi.

**Các screen chưa áp t()**:
- `app/(tabs)/calendar.tsx` — tab Lịch
- `app/(tabs)/report.tsx` — tab Báo cáo
- `app/(tabs)/budget.tsx` — tab Ngân sách
- `app/(tabs)/more.tsx` — tab Khác (menu items)
- `app/settings/categories.tsx` — Quản lý danh mục
- `app/settings/security.tsx` — Bảo mật / PIN
- `app/settings/notifications.tsx` — Nhắc nhở
- `app/settings/recurring.tsx` — Giao dịch lặp
- `app/settings/wallets.tsx` — Quản lý ví
- `app/settings/salary.tsx` — Chu kỳ lương
- `app/settings/advanced.tsx` — Cài đặt nâng cao
- `app/bills/index.tsx` — Hoá đơn
- `app/goals/index.tsx` — Mục tiêu tiết kiệm
- `app/insights/index.tsx` — Phân tích chi tiêu
- `app/summary/today.tsx` — Tóm tắt hôm nay
- `app/edit/[id].tsx` — Sửa giao dịch
- `app/onboarding/index.tsx` — Onboarding 4 màn
- `src/components/StreakBadge.tsx` — Modal streak
- `src/components/MonthSwitcher.tsx`, `WalletSwitcher.tsx`, v.v.
- `src/components/LockScreen.tsx` — Màn khoá

**Tổng**: ~250 hardcoded strings cần dịch.

### Bug B — Phải thoát app mới đổi
**Nguyên nhân**: `useLocale()` em viết có sync `setLocale(locale)` trong `useEffect` → chạy SAU render. Lúc render đầu, `t()` đọc locale CŨ.

**Fix**: Thay vì `useEffect`, dùng `useStore(s => s.settings.locale)` selector → component re-render khi store đổi → `t()` đọc locale mới.

### Bug C — Tiếng Trung chưa có
- Chỉ có `vi.ts` + `en.ts`. Cần thêm `zh.ts` cho tiếng Trung giản thể (Simplified Chinese).
- Update `AVAILABLE_LOCALES` thêm `{code:'zh', label:'中文', flag:'🇨🇳'}`.

---

## 2. Solution

### Strategy A — Hook `useT()` thống nhất

Tạo hook `useT()` thay thế `useLocale()`:

```ts
// src/i18n/useT.ts
import { useStore } from '../store/useStore';
import { setLocale, t as tRaw, Locale } from './index';

export function useT() {
  const localeRaw = useStore(s => s.settings.locale);
  const locale: Locale = localeRaw === 'en' ? 'en' : localeRaw === 'zh' ? 'zh' : 'vi';
  // Sync ngay trong render (không qua useEffect) để t() đọc đúng locale
  setLocale(locale);
  return tRaw;
}
```

Mỗi component cần dịch:
```tsx
const t = useT();
return <Text>{t('input.title.expense')}</Text>;
```

→ Bất kỳ chỗ nào pick ngôn ngữ mới → store update → component subscribed re-render → `t()` đọc locale mới ngay (không cần thoát app).

### Strategy B — UX language screen có "Xác nhận" + reload feedback

Screen `app/settings/language.tsx` đổi UX:
- Hiện 3 option (radio-style)
- User pick → highlight nhưng chưa apply
- Có nút "Áp dụng" / "Apply" / "应用" lớn bên dưới
- Bấm → `updateSetting('locale', l)` + Alert "Đã đổi sang [ngôn ngữ]" → toàn app re-render

Hoặc đơn giản: pick là apply liền (UX TikTok), không cần nút riêng. Nhưng đại ca yêu cầu có nút → em làm nút.

### Strategy C — Thêm tiếng Trung

File mới `src/i18n/zh.ts` mirror toàn bộ keys với bản dịch tiếng Trung giản thể.

Ví dụ:
- `tab.input` → "记录"
- `tab.calendar` → "日历"
- `tab.report` → "报告"
- `tab.budget` → "预算"
- `tab.more` → "更多"
- `input.title.expense` → "记录支出"
- `safe.morning` → "今早可以花"

Em tự dịch (có thể dùng standard fintech vocab tiếng Trung — không cần native speaker review).

---

## 3. Effort estimate

| Task | Effort | Risk |
|---|---|---|
| Tạo `useT()` hook + refactor `useLocale` | 15p | Low |
| Add `zh.ts` (~70 keys translated) | 30p | Low |
| Update `AVAILABLE_LOCALES` + parseLocale logic | 5p | Low |
| Screen language: 3 option + nút Áp dụng | 20p | Low |
| Áp `useT()` + `t()` vào ~30 file (250 strings) | 2.5h | Medium — nhiều file dễ miss |
| Test toàn app 3 ngôn ngữ | 30p | Low |
| Update BUILD_SPEC | 10p | - |
| **Tổng** | **~4h** | |

Em recommend chia 2 PHASE để đại ca duyệt được từng bước:

### Phase 1 (1.5h) — Foundation + critical screens
- `useT()` hook
- File `zh.ts`
- Update language screen với nút Áp dụng + 3 ngôn ngữ
- Áp `t()` vào: tab Lịch, Báo cáo, Ngân sách, Khác (menu items), 4 settings sub chính (categories, security, notifications, salary)
- → User sẽ thấy 90% UI đổi qua 3 ngôn ngữ

### Phase 2 (1.5h sau) — Long-tail
- Bills, goals, insights, summary, edit, onboarding
- Các component (StreakBadge, MonthSwitcher, LockScreen, ErrorBoundary, ResetModal)
- Strings trong store/service notifications

---

## 4. Bộ keys mới cần thêm (cho Phase 1)

```ts
// vi.ts mới
'more.balance': 'Tổng số dư',
'more.bills.empty': 'Không có bill',
'more.bills.urgent': '{count} bill sắp đến hạn',
'more.goals.empty': 'Chưa có goal',
'more.goals.active': '{count} mục tiêu đang chạy',
'more.summaryToday': 'Tóm tắt hôm nay',
'more.summaryTodayDesc': 'Tổng kết chi tiêu + streak — 1 phút thôi',
'more.bills': 'Hoá đơn sắp đến hạn',
'more.billsDesc': 'Tiền nhà, internet, gói data... + auto pay',
'more.wallets': 'Quản lý ví',
'more.walletsDesc': 'Tiền mặt, thẻ ngân hàng, MoMo, ZaloPay...',
'more.goals': 'Mục tiêu tiết kiệm',
'more.goalsDesc': 'Đặt mục tiêu (iPhone, du lịch...) + tracking',
'more.salary': 'Chu kỳ lương',
'more.salaryDesc': 'Tính số dư an toàn theo ngày nhận lương',
'more.settings': 'Cài đặt',
'more.settingsDesc': 'Giao diện, ngôn ngữ, sao lưu, quản lý dữ liệu',
'more.rate': 'Đánh giá ứng dụng',
'more.rateDesc': '5 sao nếu bạn thấy hữu ích',
'more.contact': 'Liên hệ phản hồi',
'more.contactDesc': 'Báo lỗi hoặc đề xuất tính năng',

// Report
'report.title': 'Báo cáo',
'report.aiCta': 'Phân tích chi tiêu tháng này',
'report.aiCtaSub': 'Bất thường · Gợi ý cụ thể · Tiết kiệm',
'report.viewMonth': 'Tháng',
'report.view6Month': '6 tháng',
'report.viewYear': 'Năm',
'report.expense': 'Chi',
'report.income': 'Thu',
'report.noData': 'Chưa có dữ liệu',
'report.noDataDesc': 'Ghi vài giao dịch chi để báo cáo có nội dung',
'report.avgPerDay': 'Trung bình/ngày',
'report.prevMonth': 'Tháng trước',
'report.biggest': 'Lớn nhất',
'report.compareWeek': 'So với tuần trước',

// Calendar
'calendar.title': 'Lịch',
'calendar.totalExp': 'Tổng chi',
'calendar.totalInc': 'Tổng thu',
'calendar.txCount': '{n} giao dịch',
'calendar.deleted': 'Đã xoá',
'calendar.undo': 'Hoàn tác',

// Budget
'budget.title': 'Ngân sách',
'budget.addBudget': 'Đặt ngân sách',
'budget.empty': 'Chưa đặt ngân sách nào',
'budget.over': 'Vượt ngân sách',
'budget.percent': '{n}%',

// Language screen
'language.title': 'Ngôn ngữ',
'language.hint': 'Chọn ngôn ngữ giao diện. Dữ liệu giao dịch (danh mục VN) không đổi.',
'language.apply': 'Áp dụng',
'language.applied': 'Đã đổi sang {lang}',
'language.note': 'Bản dịch đang hoàn thiện dần qua các bản cập nhật',
```

Tương tự `en.ts` + `zh.ts`.

---

## 5. Mock UI screen Language mới

```
┌─────────────────────────────┐
│ ←     Ngôn ngữ              │
├─────────────────────────────┤
│ Chọn ngôn ngữ giao diện.    │
│ Dữ liệu giao dịch không đổi.│
│                              │
│ ┌─────────────────────────┐ │
│ │ 🇻🇳  Tiếng Việt    ⦿    │ │  ← selected (đang chọn nhưng chưa apply)
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ 🇬🇧  English      ○     │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ 🇨🇳  中文          ○    │ │
│ └─────────────────────────┘ │
│                              │
│ ┌─────────────────────────┐ │
│ │      ÁP DỤNG            │ │  ← nút lớn dưới, disabled nếu chưa pick khác
│ └─────────────────────────┘ │
│                              │
│ Bản dịch đang hoàn thiện dần │
└─────────────────────────────┘
```

User pick → highlight bằng border + radio ⦿. Nút "Áp dụng" disabled nếu giống locale hiện tại.

Bấm Áp dụng → updateSetting('locale', picked) → toast "Đã đổi sang English" → toàn app re-render.

---

## 6. File phải sửa (Phase 1)

1. **Tạo mới**:
   - `src/i18n/useT.ts`
   - `src/i18n/zh.ts`

2. **Update**:
   - `src/i18n/index.ts` — thêm zh, refactor `t()` đảm bảo đọc locale mới
   - `src/i18n/vi.ts` — thêm ~50 keys mới (more, report, calendar, budget, language)
   - `src/i18n/en.ts` — mirror
   - `app/settings/language.tsx` — UX mới với nút Áp dụng + 3 option
   - `app/(tabs)/calendar.tsx` — áp t()
   - `app/(tabs)/report.tsx` — áp t()
   - `app/(tabs)/budget.tsx` — áp t()
   - `app/(tabs)/more.tsx` — áp t() cho 8 menu items
   - `app/settings/categories.tsx` — áp t()
   - `app/settings/security.tsx` — áp t()
   - `app/settings/notifications.tsx` — áp t()
   - `app/settings/salary.tsx` — áp t()
   - Files đã làm trước (kept): `_layout.tsx`, `tabs/_layout.tsx`, `tabs/index.tsx`, `settings/index.tsx`, `SafeToSpendCard.tsx`
   - Convert `useLocale` → `useT` cho consistency

3. **Phase 2 (làm sau)**:
   - bills, goals, insights, summary, edit, onboarding, recurring, wallets, advanced, StreakBadge, MonthSwitcher, LockScreen

---

## 7. Câu hỏi đại ca cần trả lời

1. **OK với plan 2 phase không?** Phase 1 ~1.5h, Phase 2 ~1.5h sau.
2. **Tiếng Trung simplified (giản thể 🇨🇳)** hay traditional (繁體 🇹🇼) hay cả 2?
3. **Nút "Áp dụng" lớn dưới** OK chứ? Hay đại ca muốn pick là apply liền (không cần nút) — bỏ confirmation step?
4. Khi đổi ngôn ngữ, có cần **toast/Alert** "Đã đổi sang [language]" không, hay silent?

---

**EM CHƯA CODE GÌ. Chờ đại ca trả lời 4 câu rồi em build Phase 1.**
